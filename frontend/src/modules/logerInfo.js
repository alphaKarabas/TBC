const logerDefaultData = {}

const logerTypes = {
  inputs: [
    {
      key: 'data',
    }
  ],
  outputs: [],
}

const logerDefaultHandles = {
  targets: [
    'target'
  ],
  sources: [
    'main-source',
  ],
}

const logerDefaultOutputKeys = {}

export {
  logerDefaultOutputKeys,
  logerDefaultData,
  logerDefaultHandles,
  logerTypes
};