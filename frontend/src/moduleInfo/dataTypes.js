import {commandTypes} from "../modules/commandInfo";
import {logerTypes} from "../modules/logerInfo";
import {textTypes} from "../modules/textInfo";
import {textMassageTypes} from "../modules/textMassageInfo";
import {questionTypes} from "../modules/questionInfo";

export default {
    'command': commandTypes,
    'loger': logerTypes,
    'text': textTypes,
    'textMassage': textMassageTypes,
    'question': questionTypes,
}